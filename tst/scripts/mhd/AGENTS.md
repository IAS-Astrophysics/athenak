# Directory Guide

## Role
Legacy script-based MHD regression checks.

## Important Files
- `mhd_linwave.py`: linear-wave style convergence test for the older harness.

## Read This Next
- Prefer `tst/test_suite/` for new automated coverage unless you specifically need the old `run()/analyze()` interface.
