# Directory Guide

## Role
Shared helpers for the older regression harness.

## Important Files
- `athena.py`: build/run wrappers for compiling AthenaK and launching test cases.
- `log_pipe.py`: logging adapter used to capture subprocess output.

## Read This Next
- The newer pytest suite has a different helper layer in `tst/test_suite/testutils.py`.
