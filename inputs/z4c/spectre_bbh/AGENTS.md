# Directory Guide

## Role
Input deck for loading binary black hole initial data exported from SpECTRE.

## Important Files
- `z4c_spectre_bbh.athinput`: SpECTRE-backed BBH setup; use this when touching external initial-data import paths.

## Read This Next
- For CMake/library wiring, read the root `CMakeLists.txt`.
- For evolution/output behavior, read `src/z4c/AGENTS.md`.
